# ignore everything (dirs, files, sub-dirs, sub-files)
/*
# but do not ignore the .gitignore file
!.gitignore
!README.md

# do not ignore dir (needed due to the above bullet point)
!/game_2048
# ignore files and sub dirs in the directory
/game_2048/*

# but do not ignore .qpf and .sv files
!/game_2048/*.sv
!/game_2048/*.qpf

# do not ignore ALU dir (needed due to the above bullet point)
!/'Laboratorio 2'/*
# ignore files and sub dirs in the /ALU directory
/'Laboratorio 2'/*/*

# but do not ignore .qpf and .sv files
!/'Laboratorio 2'/*/*.sv
!/'Laboratorio 2'/*/*.qpf
